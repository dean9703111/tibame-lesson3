# pnpm 在 workspace monorepo 中預設使用虛擬存放（.pnpm/），套件不會出現在 root node_modules/。
# apps/api 的 test script 直接以相對路徑引用 ../../node_modules/jest/bin/jest.js，
# 因此 jest、ts-jest、@types/jest 已移至 root devDependencies（root 直接依賴一定在 root node_modules/）。
# 無需額外的 hoist 設定。
