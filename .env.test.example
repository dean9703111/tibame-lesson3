# 測試專用環境變數範本。第一次：cp .env.test.example .env.test。
# 執行 `npm test` 時，NODE_ENV=test 會讓 api 在 .env 之上再疊加本檔（override）。
# 只需覆蓋要與 dev 區隔的鍵（這裡是 DATABASE_URL），其餘自動沿用 .env。
#
# ⚠️ DATABASE_URL 指向與 dev 同一個 Postgres 容器（vms-db）內的獨立資料庫 vms_test，
# 讓測試永遠不會污染 dev 資料庫；pgAdmin 連上同一 server 即可在樹狀清單看到 vms_test。
DATABASE_URL=postgresql://vms:vms@localhost:5432/vms_test?schema=public
